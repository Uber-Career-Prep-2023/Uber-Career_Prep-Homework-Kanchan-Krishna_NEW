#include <iostream.h>
//search on Google for this error 
//Given a number, k, return an array of the first k binary numbers, represented as strings.
/*
The brute force way is to use a series of helper methods; iterate until and including k; call a method to generate the binary version of that number k; convert it to a string; push it back to a vector 
*/
